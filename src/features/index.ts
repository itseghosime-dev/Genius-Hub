/**
 * Feature Domains Directory
 *
 * This directory holds domain-specific feature modules. Each feature folder encapsulates:
 * - components/ (feature-specific UI components)
 * - hooks/ (feature-specific React hooks)
 * - api/ or actions/ (feature server actions and API clients)
 * - types/ (feature domain contracts)
 * - utils/ (feature domain helpers)
 *
 * Future feature domains planned for Genius Hub:
 * - auth (Authentication, MFA, session management)
 * - users (User accounts, staff profiles, beneficiary records)
 * - content (Editorial publishing, blog, success stories, pages)
 * - programmes (Training programmes, cohorts, curriculums)
 * - applications (Beneficiary/student applications, screening workflows)
 * - events (Workshops, hackathons, event ticketing, QR verification)
 * - commerce (Physical, digital, and training product catalogue)
 * - payments (Paystack/Flutterwave gateways, receipts, ledger)
 * - donations (Charitable giving, campaigns, donor tracking)
 * - communications (Email marketing, newsletters, notification dispatch)
 * - media (Asset galleries, photo archives, CDN management)
 * - audit (Compliance audit trails, admin activity logging)
 * - seo (Dynamic schema generation, sitemap indexing, metadata)
 */
export {};

